[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    pgvector: tests that require a running Postgres + pgvector instance
    recorded: provider replay tests using recorded HTTP cassettes (offline)
